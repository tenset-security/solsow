# Solsow

Solsow helps web3 devs scaffold and manage tests in a Foundry project.

It automatically parses all your smart contract files and generates directory structure under `test/concrete` for each public function.
